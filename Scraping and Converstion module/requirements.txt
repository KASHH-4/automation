beautifulsoup4==4.15.0      # HTML/XML parser used for DOM traversal to extract product metadata
certifi==2026.7.22          # Provides Mozilla's root certificates for secure SSL connections
charset-normalizer==3.4.9   # Character encoding auto-detection (dependency of requests)
click==8.4.2                # Command line interface creation kit (dependency of duckduckgo-search)
colorama==0.4.6             # Cross-platform colored terminal text (dependency of click/rich)
duckduckgo_search==8.1.1    # DuckDuckGo search library used for universal store discovery
ddgs==9.14.4                # Updated version of duckduckgo_search to avoid warnings
greenlet==3.5.4             # Lightweight concurrent programming (dependency of playwright)
idna==3.18                  # Internationalized Domain Names support (dependency of requests)
lxml==6.1.1                 # High-performance C-based XML/HTML parser (dependency of duckduckgo-search)
markdown-it-py==4.2.0       # Markdown parser (dependency of rich)
mdurl==0.1.2                # Markdown URL utilities (dependency of markdown-it-py)
playwright==1.61.0          # Headless browser automation framework used as a fallback for JS-heavy/bot-protected sites
primp==1.3.1                # Python requests wrapper (dependency of duckduckgo-search)
pyee==13.0.1                # Event emitter for Python (dependency of playwright)
Pygments==2.20.0            # Generic syntax highlighter (dependency of rich)
requests==2.34.2            # Standard library for making fast synchronous HTTP requests
rich==15.0.0                # Terminal formatting library used for the live progress bar and CLI dashboard UI
selectolax==0.4.11          # Extremely fast HTML5 parser (alternative parsing engine for Python)
soupsieve==2.9.1            # CSS selector library (dependency of beautifulsoup4)
typing_extensions==4.16.0   # Backported and experimental type hints
urllib3==2.7.0              # Thread-safe HTTP library with connection pooling (dependency of requests)